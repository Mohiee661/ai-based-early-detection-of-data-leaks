# UI Extension Notes

This frontend is prepared for future `21st.dev` installs through the `shadcn` CLI.

Use:

`npx shadcn@latest add https://21st.dev/r/<component-id>`

That command will drop the component into the existing `src/components/ui` structure and reuse the shared `cn()` helper in `src/lib/utils.ts`.
